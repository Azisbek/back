# 📝 API для формы заявки

## Endpoint

**POST** `/api/application/`

## Описание

Отправляет заявку с ФИО, номером телефона и сообщением. Заявка сохраняется в базе данных и становится доступной в админ-панели для обработки менеджерами.

## Поля запроса

| Поле | Тип | Обязательное | Описание |
|------|-----|-------------|----------|
| `full_name` | string | ✅ | ФИО заявителя (минимум 2 символа) |
| `phone` | string | ✅ | Номер телефона (минимум 10 цифр) |
| `message` | string | ✅ | Сообщение от заявителя |

## Пример запроса

### JavaScript/React

```javascript
const applicationData = {
  full_name: 'Иванов Иван Иванович',
  phone: '+7 (999) 123-45-67',
  message: 'Интересует разработка корпоративного сайта. Просьба связаться для обсуждения деталей и стоимости.'
};

const response = await fetch('http://127.0.0.1:8000/api/application/', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify(applicationData)
});

const result = await response.json();

if (response.ok) {
  console.log('Заявка отправлена:', result.message);
  console.log('ID заявки:', result.id);
} else {
  console.error('Ошибка:', result);
}
```

### Python

```python
import requests

application_data = {
    'full_name': 'Иванов Иван Иванович',
    'phone': '+7 (999) 123-45-67',
    'message': 'Интересует разработка корпоративного сайта.'
}

response = requests.post(
    'http://127.0.0.1:8000/api/application/',
    json=application_data
)

if response.status_code == 201:
    result = response.json()
    print(f"Заявка отправлена: {result['message']}")
    print(f"ID заявки: {result['id']}")
else:
    print(f"Ошибка: {response.json()}")
```

### cURL

```bash
curl -X POST http://127.0.0.1:8000/api/application/ \
  -H "Content-Type: application/json" \
  -d '{
    "full_name": "Иванов Иван Иванович",
    "phone": "+7 (999) 123-45-67",
    "message": "Интересует разработка корпоративного сайта."
  }'
```

## Ответы

### Успешная отправка (201 Created)

```json
{
  "message": "Заявка успешно отправлена! Мы свяжемся с вами в ближайшее время.",
  "id": 1
}
```

### Ошибка валидации (400 Bad Request)

```json
{
  "full_name": ["ФИО должно содержать минимум 2 символа"],
  "phone": ["Номер телефона слишком короткий"]
}
```

## Валидация

- **ФИО**: минимум 2 символа, автоматически обрезаются пробелы
- **Телефон**: минимум 10 цифр (после удаления всех символов кроме цифр и +)
- **Сообщение**: обязательное поле

## Управление заявками

Все заявки доступны в админ-панели по адресу:
**http://127.0.0.1:8000/admin/website/applicationform/**

### Возможности админки:

- ✅ **Просмотр всех заявок** с фильтрацией и поиском
- 📊 **Статусы обработки**: "Обработано", "Связались"
- 📝 **Заметки менеджера** для каждой заявки
- 🔍 **Поиск** по ФИО, телефону и сообщению
- 📅 **Фильтрация** по дате создания и статусам
- ⚡ **Массовые действия** для обработки нескольких заявок

### Поля в админке:

- **ФИО** - имя заявителя
- **Телефон** - контактный номер
- **Сообщение** - текст заявки
- **Дата создания** - когда была отправлена заявка
- **Обработано** - отметка о том, что заявка просмотрена
- **Связались** - отметка о том, что с клиентом связались
- **Заметки менеджера** - внутренние комментарии

## Интеграция с фронтендом

### Пример React компонента

```jsx
import React, { useState } from 'react';

const ApplicationForm = () => {
  const [formData, setFormData] = useState({
    full_name: '',
    phone: '',
    message: ''
  });
  const [isSubmitting, setIsSubmitting] = useState(false);
  const [message, setMessage] = useState('');

  const handleSubmit = async (e) => {
    e.preventDefault();
    setIsSubmitting(true);

    try {
      const response = await fetch('/api/application/', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify(formData)
      });

      const result = await response.json();

      if (response.ok) {
        setMessage('Заявка успешно отправлена!');
        setFormData({ full_name: '', phone: '', message: '' });
      } else {
        setMessage('Ошибка при отправке заявки');
      }
    } catch (error) {
      setMessage('Произошла ошибка');
    } finally {
      setIsSubmitting(false);
    }
  };

  return (
    <form onSubmit={handleSubmit}>
      <input
        type="text"
        placeholder="ФИО"
        value={formData.full_name}
        onChange={(e) => setFormData({...formData, full_name: e.target.value})}
        required
      />
      <input
        type="tel"
        placeholder="Номер телефона"
        value={formData.phone}
        onChange={(e) => setFormData({...formData, phone: e.target.value})}
        required
      />
      <textarea
        placeholder="Сообщение"
        value={formData.message}
        onChange={(e) => setFormData({...formData, message: e.target.value})}
        required
      />
      <button type="submit" disabled={isSubmitting}>
        {isSubmitting ? 'Отправка...' : 'Отправить заявку'}
      </button>
      {message && <p>{message}</p>}
    </form>
  );
};
```

## Тестирование в Swagger UI

Откройте http://127.0.0.1:8000/api/docs/ и найдите раздел **"Application"** для интерактивного тестирования API.
