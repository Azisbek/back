# 📚 Руководство по использованию Swagger API

## Быстрый старт

После запуска Django сервера (`python manage.py runserver`) откройте в браузере:

**🔗 http://127.0.0.1:8000/api/docs/**

## Основные возможности

### 🎯 Тестирование API

1. **Выберите endpoint** из списка (например, `GET /api/website-data/`)
2. **Нажмите "Try it out"** 
3. **Заполните параметры** (если требуются)
4. **Нажмите "Execute"** для отправки запроса
5. **Просмотрите результат** в разделе "Response"

### 📋 Группы endpoints

API организован по следующим группам:

- **Website Data** - Получение всех данных сайта одним запросом
- **Hero Section** - Управление главными баннерами
- **About** - Информация о компании
- **Partners** - Партнеры компании
- **Team** - Команда сотрудников
- **Services** - Услуги компании
- **Insights** - Статьи и блог
- **Contact** - Контактная информация и обратная связь

### 🔍 Фильтрация статей

Endpoint `/api/insights/` поддерживает фильтры:

```
GET /api/insights/?category=tech&featured=true
```

- `category` - фильтр по категории (tech, business, design)
- `featured` - показать только рекомендуемые статьи (true/false)

### 📝 Отправка сообщений

Для тестирования формы обратной связи используйте `POST /api/contact/message/`:

```json
{
  "name": "Иван Иванов",
  "email": "ivan@example.com",
  "subject": "Вопрос по услугам",
  "message": "Здравствуйте! Интересует разработка сайта.",
  "phone": "+7 (999) 123-45-67"
}
```

## Альтернативные интерфейсы

### ReDoc (красивая документация)
**🔗 http://127.0.0.1:8000/api/redoc/**

### OpenAPI Schema (для разработчиков)
**🔗 http://127.0.0.1:8000/api/schema/**

## Примеры использования в коде

### JavaScript/React

```javascript
// Получение всех данных сайта
const response = await fetch('http://127.0.0.1:8000/api/website-data/');
const data = await response.json();

// Отправка сообщения
const messageData = {
  name: 'Имя',
  email: 'email@example.com',
  subject: 'Тема',
  message: 'Сообщение'
};

const response = await fetch('http://127.0.0.1:8000/api/contact/message/', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify(messageData)
});
```

### Python

```python
import requests

# Получение данных
response = requests.get('http://127.0.0.1:8000/api/website-data/')
data = response.json()

# Отправка сообщения
message_data = {
    'name': 'Имя',
    'email': 'email@example.com',
    'subject': 'Тема',
    'message': 'Сообщение'
}

response = requests.post(
    'http://127.0.0.1:8000/api/contact/message/',
    json=message_data
)
```

## Полезные советы

1. **Используйте фильтры** в Swagger UI для быстрого поиска нужных endpoints
2. **Сохраняйте примеры** запросов для повторного использования
3. **Проверяйте коды ответов** - 200 (успех), 400 (ошибка запроса), 404 (не найдено)
4. **Обращайте внимание на типы данных** в схемах запросов и ответов

## Поддержка

Если возникли вопросы по API, проверьте:
1. Запущен ли Django сервер
2. Правильность URL endpoints
3. Формат отправляемых данных
4. Логи сервера в терминале
