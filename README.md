# Django Backend для корпоративного сайта

Это Django backend для управления контентом корпоративного сайта с админ-панелью.

## Возможности

- 🏠 **Hero секции** - управление главными баннерами
- 📖 **О компании** - информация о компании, миссии, видении
- 🤝 **Партнеры** - логотипы и информация о партнерах
- 👥 **Команда** - профили сотрудников с фото и контактами
- 💼 **Услуги** - каталог услуг с ценами и описаниями
- 📰 **Статьи/Инсайты** - блог с категориями и статьями
- 📞 **Контакты** - контактная информация и форма обратной связи
- 🔧 **Админ-панель** - удобное управление всем контентом

## Быстрый запуск

### Автоматическая установка и запуск (рекомендуется)

```bash
# Запустите скрипт автоматической установки
./start.sh
```

Скрипт автоматически:
- Создаст виртуальное окружение
- Установит все зависимости
- Создаст и применит миграции
- Предложит создать суперпользователя
- Запустит сервер

### Ручная установка

#### 1. Установка зависимостей

```bash
# Создание виртуального окружения
python3 -m venv venv

# Активация виртуального окружения
# На Windows:
venv\\Scripts\\activate
# На macOS/Linux:
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

#### 2. Настройка базы данных

```bash
# Создание миграций
python manage.py makemigrations

# Применение миграций
python manage.py migrate

# Создание суперпользователя для админки
python manage.py createsuperuser
```

#### 3. Загрузка примеров данных (опционально)

```bash
# Загрузка тестовых данных
python manage.py shell < load_sample_data.py
```

#### 4. Запуск сервера

```bash
python manage.py runserver
```

### Доступ к приложению

- **Сервер**: http://127.0.0.1:8000/
- **Админка**: http://127.0.0.1:8000/admin/
- **API**: http://127.0.0.1:8000/api/
- **📚 Swagger UI**: http://127.0.0.1:8000/api/docs/
- **📖 ReDoc**: http://127.0.0.1:8000/api/redoc/
- **🔧 OpenAPI Schema**: http://127.0.0.1:8000/api/schema/

## 📚 Swagger/OpenAPI Документация

Проект включает полную интерактивную документацию API:

### 🔧 Доступные интерфейсы:

- **Swagger UI** (http://127.0.0.1:8000/api/docs/) - интерактивная документация с возможностью тестирования API прямо в браузере
- **ReDoc** (http://127.0.0.1:8000/api/redoc/) - красивая документация в стиле ReDoc
- **OpenAPI Schema** (http://127.0.0.1:8000/api/schema/) - JSON/YAML схема для интеграции с другими инструментами

### 🎯 Возможности Swagger UI:

- ✅ Тестирование всех API endpoints
- 📝 Подробные описания каждого метода
- 🔍 Фильтры и параметры запросов
- 📊 Примеры запросов и ответов
- 🏷️ Группировка по тегам (Hero, About, Partners, Team, Services, Insights, Contact)
- 🚀 Отправка реальных запросов к API

## API Endpoints

### Основные endpoints:

- `GET /api/website-data/` - Получение всех данных сайта одним запросом
- `GET /api/hero/` - Hero секции
- `GET /api/about/` - Информация о компании
- `GET /api/partners/` - Список партнеров
- `GET /api/team/` - Команда
- `GET /api/services/` - Все услуги
- `GET /api/services/featured/` - Рекомендуемые услуги
- `GET /api/insights/` - Статьи (с фильтрами ?category=slug&featured=true)
- `GET /api/insights/<slug>/` - Детальная информация о статье
- `GET /api/contact/` - Контактная информация
- `POST /api/contact/message/` - Отправка сообщения
- `POST /api/application/` - Отправка заявки (ФИО, телефон, сообщение)

### Пример использования в React:

```javascript
// Получение всех данных сайта
const response = await fetch('http://127.0.0.1:8000/api/website-data/');
const data = await response.json();

// Отправка сообщения
const messageData = {
  name: 'Имя',
  email: 'email@example.com',
  subject: 'Тема',
  message: 'Текст сообщения',
  phone: '+7 (999) 123-45-67'
};

const response = await fetch('http://127.0.0.1:8000/api/contact/message/', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify(messageData)
});

// Отправка заявки
const applicationData = {
  full_name: 'Иванов Иван Иванович',
  phone: '+7 (999) 123-45-67',
  message: 'Интересует разработка корпоративного сайта'
};

const appResponse = await fetch('http://127.0.0.1:8000/api/application/', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify(applicationData)
});
```

## Админ-панель

Админ-панель доступна по адресу: http://127.0.0.1:8000/admin/

### Возможности админки:

- ✅ Удобное управление всем контентом
- 🖼️ Превью изображений
- 📊 Фильтры и поиск
- 🔄 Массовые операции
- 📈 Статистика просмотров статей
- 📧 Управление сообщениями обратной связи
- 📝 **Расширенное управление заявками**:
  - 🔍 Продвинутый поиск и фильтрация
  - 📊 Статистика по статусам заявок
  - 📈 Экспорт данных в CSV
  - ⚡ Быстрое редактирование статусов
  - 🎯 Массовые действия для обработки
  - 📋 Детальные заметки менеджера

## Структура проекта

```
backend/
├── backend/           # Настройки Django проекта
│   ├── settings.py   # Основные настройки
│   ├── urls.py       # Главные URL
│   └── wsgi.py       # WSGI конфигурация
├── website/          # Основное приложение
│   ├── models.py     # Модели данных
│   ├── admin.py      # Настройки админки
│   ├── views.py      # API views
│   ├── serializers.py # DRF сериализаторы
│   └── urls.py       # URL приложения
├── manage.py         # Django управление
└── requirements.txt  # Зависимости
```

## Настройка переменных окружения

Скопируйте `env.example` в `.env` и настройте переменные:

```bash
cp env.example .env
```

## Развертывание

Для продакшена рекомендуется:

1. Использовать PostgreSQL вместо SQLite
2. Настроить статические файлы через веб-сервер
3. Использовать gunicorn для запуска
4. Настроить HTTPS
5. Отключить DEBUG режим

## 📚 Дополнительная документация

- **📝 Руководство по API заявок**: `APPLICATION_API.md`
- **📋 Руководство по админ-панели**: `ADMIN_GUIDE.md`
- **📚 Руководство по Swagger**: `SWAGGER_GUIDE.md`

## Поддержка

Если у вас есть вопросы или предложения, создайте issue в репозитории.
